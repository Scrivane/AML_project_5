COSE DA FARE

base

sempre mantenendo 100 clients, variare il numero di Nc (random da 1 a 50) per ogni client. Ogni client avra cosi da 1 a 50 classi al suo interno e sara piu simile al mondo reale.







client selection

selezionare client in base alla diversita del client stesso (diversity parameter => premia la varianza interna del client [aka quante classi sara composto il piu delle volte])

selezionare client in base all'utilita verso il modello (controllo quanto abbassa la loss addestrare quel client, se la loss di partenza è alta allora conviene usarlo, se è gia bassa lo si salta)




variare il local step per client in base all utilita riportata da client a client (se la loss sta scendendo velocemente allora faremo meno local step, se ci mette di piu allora fai piu passi J). valutare se mettere un hard limit al valore della loss e un hard limit al valore di j





